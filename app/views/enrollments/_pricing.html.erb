Pay <%= number_to_currency(Enrollment::BASE_PRICE) %> at enrollment or make 
<%= Enrollment::PAYMENT_COUNT %> payments of 
<%= number_to_currency(Enrollment::PAYMENT_PRICE) %>. 
Pacing does not effect price.
